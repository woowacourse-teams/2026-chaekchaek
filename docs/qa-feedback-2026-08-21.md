# 2026-08-21 QA 피드백 검증

## 체크 기준

- `[x]`: `an-develop`에서 제보된 문제가 이미 해결된 것으로 확인됨
- `[ ]`: 문제가 현재도 발생하거나 로그인 계정, 선행 데이터, 원본 첨부 파일 부족으로 해결 여부를 확인하지 못함
- 소스에 수정 구현이 있어도 실제 동작을 확인하지 못한 항목은 체크하지 않는다.

## 검증 기준과 환경

- 기준 브랜치: `origin/an-develop`
- 기준 커밋: `79902fcf4560b56da47d3aaa08e4598677ccc62b`
- 검증일: 2026-08-25
- 실행 환경: Android `medium_phone` AVD, 로그아웃 상태
- 라이브 API: `https://api.chaekchaek.com`
- 원본 영상과 이미지 파일은 worktree에서 찾을 수 없어 사용자 설명과 현재 화면만 대조했다.

### 실행 결과

- `./gradlew test`: 통과
- `./gradlew :shared:allTests`: 통과
- `./gradlew :app:connectedDebugAndroidTest`: 기기 오프라인으로 실패, 테스트 0건 실행
  - 앱 설치와 수동 화면 검증 이후 AVD가 오프라인으로 전환됐다.
  - 기기 테스트에만 있는 평점 기록 저장 검증은 이번 결과의 근거로 사용하지 않았다.

## 홈 페이지

- [x] **HOME-01 비로그인 상태에서 읽는 중 선택 시 로그인 유도가 나오지 않음**
  - 결과: 미재현
  - `프리즘` 상세 화면에서 `읽는 중`을 선택하면 `로그인이 필요해요` 시트가 표시됐다.

- [ ] **HOME-02 서재에서 삭제한 책이 홈의 이어서 읽기에 남아 있음**
  - 결과: 확인 불가
  - 로그인 계정과 서재 데이터가 필요해 삭제 이후 홈 피드 갱신까지 확인하지 못했다.

## 검색 페이지

- [ ] **SEARCH-01 검색 결과에서 읽는 중 등록 후 상세 페이지로 이동하지 않음**
  - 결과: 확인 불가
  - 로그아웃 상태에서는 로그인 시트가 정상 표시됐다. 로그인 후 등록과 이동은 확인하지 못했다.

- [ ] **SEARCH-02 검색 결과가 10개에서 끝나고 추가 로딩되지 않음**
  - 결과: 재현
  - `javascript` 검색 화면은 `검색 결과 10건`만 표시했고 끝까지 스크롤해도 추가 결과를 불러오지 않았다.
  - 같은 조건의 라이브 API 2페이지 응답은 `totalCount: 296`, `nextPage: 3`과 별도 결과를 반환했다.
  - 앱은 항상 `page=1`만 요청하고 응답의 `nextPage`와 `totalCount`를 모델링하지 않는다.
  - 코드 근거: `android/shared/src/commonMain/kotlin/com/chaekchaek/app/data/remote/BookSearchRemoteRepository.kt:15`

- [x] **SEARCH-03 최신순 정렬을 선택해도 결과가 변경되지 않음**
  - 결과: 미재현
  - 정렬 메뉴에서 `감상 많은순`을 선택하면 라벨과 결과 순서가 변경됐다.

## 상세 페이지

### 책 정보와 서재 등록

- [ ] **DETAIL-01 홈에서 상세 페이지로 진입해 책을 추가하면 오류 발생**
  - 결과: 확인 불가
  - 홈에서 `프리즘` 상세 페이지로 진입했고 로그아웃 상태의 `서재에 추가`는 오류 대신 로그인 시트를 표시했다.
  - 로그인 후 실제 추가 요청은 확인하지 못했다.

- [ ] **DETAIL-02 상세 페이지에서 읽는 중 선택 시 요청 실패 메시지 표시**
  - 결과: 확인 불가
  - 로그아웃 상태에서는 요청 실패 메시지 대신 로그인 시트가 표시됐다. 로그인 후 요청은 확인하지 못했다.

- [ ] **DETAIL-03 상세 정보가 좌우 공간 부족으로 잘림**
  - 결과: 재현
  - `프리즘`과 검색 결과 도서 상세 화면에서 가로 메타 정보의 오른쪽 평점 텍스트가 화면 끝에서 잘렸다.
  - 메타 정보가 중앙 정렬된 가로 스크롤 Row에 들어 있어 초기 위치에서 오른쪽 내용이 잘려 보인다.
  - 코드 근거: `android/app/src/main/java/com/chamsae/chaekchaek/ui/bookdetail/BookDetailScreen.kt:680`

### 감상 정렬과 작성

- [ ] **DETAIL-04 최신순과 페이지순 선택지가 펼쳐지는 UI 필요**
  - 결과: 현재 개선 요청과 동일
  - `최신순`을 선택하면 메뉴가 펼쳐지지 않고 즉시 `페이지순`으로 바뀌었다.
  - 아래 화살표 아이콘이 있지만 실제 구현은 두 값을 바로 전환한다.
  - 코드 근거: `android/app/src/main/java/com/chamsae/chaekchaek/ui/bookdetail/BookDetailScreen.kt:869`

- [ ] **DETAIL-05 비로그인 상태에서 답글 작성 후 등록 시 로그인 유도**
  - 결과: 정책 확인 필요
  - 현재 구현도 답글 입력은 먼저 열고 등록 시 로그인시키는 구조다.
  - 제보가 원하는 동작인지, 작성 시작 전에 로그인시켜야 한다는 문제 제기인지 확정할 수 없다.

- [x] **DETAIL-06 스포일러가 적용되지 않음**
  - 결과: 미재현
  - `프리즘` 상세 화면에서 스포일러 감상이 `짹`으로 가려지고 `감상 내용 잠김` 상태로 표시됐다.

- [x] **DETAIL-07 감상 남기기 하단 버튼을 보기 위해 스크롤해야 함**
  - 결과: 미재현
  - 현재 `이 순간의 감상 남기기` 버튼은 상세 화면 하단에 고정되어 표시됐다.

### 별점

- [ ] **RATING-01 별점 UI는 4점처럼 보이지만 `평점 없음`으로 표시됨**
  - 결과: 재현
  - 평점이 없는 검색 도서 상세 화면에서 `★★★★☆`와 `평점 없음`이 동시에 표시됐다.
  - 별 문자열이 평균 평점과 무관하게 `★★★★☆`로 고정되어 있다.
  - 코드 근거: `android/app/src/main/java/com/chamsae/chaekchaek/ui/bookdetail/BookDetailScreen.kt:708`

- [ ] **RATING-02 실제 평점과 채워진 별 개수가 다름**
  - 결과: 재현
  - `프리즘`은 평균 `2.5`로 표시됐지만 별 UI는 `★★★★☆`였다.
  - 3점대에 별 4개가 보인다는 제보보다 범위가 넓은 동일 원인이다.
  - 코드 근거: `android/app/src/main/java/com/chamsae/chaekchaek/ui/bookdetail/BookDetailScreen.kt:719`

- [ ] **RATING-03 0.5점을 선택해도 별의 절반이 채워지지 않음**
  - 결과: 확인 불가
  - 현재 소스에는 반쪽 너비를 잘라 표시하는 구현이 있다.
  - 로그인 후 별점 팝업을 열 수 없어 실제 렌더링은 확인하지 못했다.
  - 코드 근거: `android/app/src/main/java/com/chamsae/chaekchaek/ui/bookdetail/BookRatingDialog.kt:215`

- [ ] **RATING-04 별점을 0.1점 단위로 입력할 수 없음**
  - 결과: 현재 요구사항과 불일치 확정
  - 현재 도메인 모델과 선택 UI는 0.5점 단위만 허용한다.
  - 코드 근거: `android/shared/src/commonMain/kotlin/com/chaekchaek/app/domain/rating/Rating.kt:5`

- [ ] **RATING-05 별점 팝업의 내 평점 기록 횟수가 0으로 표시됨**
  - 결과: 확인 불가
  - 로그인 계정의 기존 평점 기록이 없어 팝업 데이터와 서버 기록을 대조하지 못했다.

- [ ] **RATING-06 별점을 선택해도 비교 별점 도서가 표시되지 않음**
  - 결과: 확인 불가
  - 로그인 계정의 평점 이력과 별점 팝업을 준비하지 못했다.

### 피드, 좋아요, 답글

- [ ] **FEED-01 전체 피드와 내 피드 전환 또는 좋아요 선택 시 스크롤이 상단으로 이동함**
  - 결과: 확인 불가
  - 내 피드와 좋아요는 로그인이 필요해 실제 동작을 확인하지 못했다.

- [ ] **FEED-02 내가 좋아요를 누른 감상에 UI 표시가 없음**
  - 결과: 확인 불가
  - 로그인 상태의 `likedByMe` 응답을 준비하지 못했다.

- [ ] **FEED-03 좋아요를 다시 선택하면 취소되지 않고 오류가 표시됨**
  - 결과: 확인 불가
  - 로그인 상태의 좋아요 등록과 취소를 실행하지 못했다.

- [ ] **FEED-04 작성한 답글을 확인하는 방법을 찾기 어려움**
  - 결과: 미재현 여부 확정 불가
  - 현재 화면에는 `답글 N` 진입점이 있고 소스에는 `N개 모두 보기`가 구현되어 있다.
  - 제보 당시의 `프리즘` 답글 데이터와 동일한 상태를 만들지 못했다.

- [ ] **FEED-05 익명 안내와 실제 감상 작성 상태가 다름**
  - 결과: 확인 불가
  - 로그인 계정의 익명 설정 변경과 감상 등록이 필요하다.

- [ ] **FEED-06 익명 해제 후에도 감상 작성 팝업에 익명 안내가 남음**
  - 결과: 확인 불가
  - 로그인 계정의 익명 설정 변경이 필요하다.

## 서재 페이지

- [ ] **LIBRARY-01 비로그인 상태에서도 편집 화면에 진입할 수 있음**
  - 결과: 재현
  - 로그아웃 상태에서 `내 서재`, `편집`을 선택하자 `0권 선택`, 익명 설정, 편집 하단 버튼이 있는 화면으로 진입했다.
  - 서재 편집 상태 변경에 인증 검사가 없다.
  - 코드 근거: `android/app/src/main/java/com/chamsae/chaekchaek/RootScreen.kt:82`, `android/app/src/main/java/com/chamsae/chaekchaek/ui/archive/ArchiveScreen.kt:151`

- [ ] **LIBRARY-02 최근 기록순을 선택해도 반응이 없음**
  - 결과: 문제 확정
  - `최근 기록순`은 클릭 처리 없이 그려지는 정적 Text다. 목록도 항상 최근 기록 내림차순으로만 계산된다.
  - 코드 근거: `android/app/src/main/java/com/chamsae/chaekchaek/ui/archive/ArchiveScreen.kt:129`, `android/app/src/main/java/com/chamsae/chaekchaek/ui/archive/ArchiveScreen.kt:374`

- [ ] **LIBRARY-03 익명 설정 변경이 서버에 반영되지 않음**
  - 결과: 확인 불가
  - 로그인 계정이 없어 익명 설정 API 요청과 재조회 결과를 확인하지 못했다.

- [ ] **LIBRARY-04 서재에서 삭제한 책이 홈의 이어서 읽기에 남아 있음**
  - 결과: 확인 불가
  - HOME-02와 같은 항목으로 로그인 계정과 서재 데이터가 필요하다.

## 검증 결과 요약

### 해결 확인 4개

1. HOME-01 비로그인 읽는 중 선택 시 로그인 유도
2. SEARCH-03 검색 결과 정렬
3. DETAIL-06 스포일러 처리
4. DETAIL-07 하단 감상 작성 버튼 고정

### 현재 문제 확인 8개

1. SEARCH-02 검색 결과 추가 로딩 누락
2. DETAIL-03 상세 메타 정보 오른쪽 잘림
3. DETAIL-04 감상 정렬 선택지가 펼쳐지지 않음
4. RATING-01 평점 없음 상태에서 별 4개 표시
5. RATING-02 실제 평균과 별 개수 불일치
6. RATING-04 0.1점 단위 입력 미지원
7. LIBRARY-01 비로그인 서재 편집 진입
8. LIBRARY-02 최근 기록순 정렬 UI 미동작

나머지 16개는 해결 여부를 확인하지 못해 체크하지 않았다.

## 객체지향 관점 확인

- 단일 책임 원칙 위반: `BookDetailRemoteRepository`가 책 상세 조회뿐 아니라 서재 변경, 별점, 감상, 답글, 좋아요 API까지 함께 담당한다.
- 코드 근거: `android/shared/src/commonMain/kotlin/com/chaekchaek/app/data/remote/BookDetailRemoteRepository.kt:18`
- 이번 작업은 QA 기록이므로 구조 변경은 하지 않았다. 관련 기능 수정 시 변경 이유가 생긴 범위에서만 분리를 검토한다.
